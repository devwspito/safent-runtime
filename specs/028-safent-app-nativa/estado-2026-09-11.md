# Estado del 11-sep-2026 — traspaso

## Dónde está cada cosa
- Runtime: `devwspito/safent-runtime`, rama `feat/safent-next`. Etiqueta `v0.9.0` puesta en `0612c41` **sin imagen construida detrás** (la construcción se canceló a petición del dueño). Borrarla o moverla antes de publicar: `git push origin :refs/tags/v0.9.0`.
- Pipeline de escritorio: `devwspito/agents-autonomy`, rama `safent-desktop-pipeline-028`, `.github/workflows/safent-desktop.yml` @ `d4a75c7e`. Flujo Tauri estándar (compilar → firmar binarios del runtime → tauri-action → notarizar DMG + grapar). Windows apagado salvo `build_windows=true`.
- Companion: `devwspito/safent-ads`, `main`. Etiqueta `v0.2.1` puesta (`e1ef451`), **imagen no construida** (cancelada). Commit posterior `e73e929` (fuera el developer token; error de nivel de acceso explícito) sin etiqueta.
- Imágenes publicadas y usables: motor `ghcr.io/devwspito/safent:v0.9.0-rc2` (arm64, `sha256:9bafbad6…`), companion `safent-ads:v0.2.0` (multi-arch). `latest` del motor sin tocar.

## Verificado en el Mac del dueño (informes `verificacion-mac-1..6.md`)
DMG firmado, notarizado y grapado; motor arranca en frío hasta `ready` (~140 s), responde desde macOS, la app sirve la interfaz con su credencial; reapertura sin recrear; recuperación tras matar el contenedor; usa los gvproxy/vfkit empaquetados; no toca `podman-machine-default`. Último artefacto verificado: run 34582572593 de agents-autonomy.

## Decisiones del dueño hoy
- SIEMPRE lo más simple: fuera la verificación propia de huellas en macOS (Apple sella el bundle: `codesign --verify --strict`); manifiesto sha256 solo en Linux.
- No lanzar el pipeline (5,38 €/pasada) salvo certeza de que sale.
- No publicar con defectos conocidos; el dueño valida la parte visual y el alta de Anuncios con sus credenciales.

## Pendiente antes de publicar
1. Alta de Anuncios: `e73e929` en safent-ads quita el token de desarrollador (Google lo retiró; el nivel de acceso va por proyecto en https://console.cloud.google.com/google/ads-apis/overview) y hace explícito `CLOUD_PROJECT_NOT_APPROVED_FOR_PRODUCTION`. Falta etiquetar `v0.2.2`, construir la imagen y verificar el flujo con credenciales reales.
2. Meta: sin confirmar si en modo desarrollo acepta el retorno OAuth por `127.0.0.1` o solo `localhost`. La redirect_uri ya se deriva del origen del navegador (safent-ads `8ede914`, puente `89ab139`); si Meta exige `localhost`, la app debe abrirse en `localhost`.
3. Secuencia de publicación cuando el dueño dé el visto bueno: tag motor → `publish-image.yml` (multi-arch) → companion etiquetado → desktop `publish=true ref=<tag> tag=<tag> companion_tag=<tag>` → release con DMG/deb/rpm + latest.json.
4. Menores: MAC6-01 (CLI sin `SAFENT_IMAGE` cae a `:latest`), `GET /platform-apps` muestra la URL base estática, MAC-08/09 del lock y `gh run download`.

## Cómo probar
- Unit: `PYTHONPATH=src python3 -m pytest tests/unit -q -p no:cacheprovider`; escritorio: `cd desktop/src-tauri && cargo test`; scripts: `bash desktop/scripts/tests/*.sh`.
- Companion: `make lint && make type && make test`.
- Mac sin pantalla: ver `verificacion-mac-6.md` (artefacto por `gh api …/artifacts/<id>/zip`, `SAFENT_STATE_HOME` propio, nunca la máquina del dueño).
