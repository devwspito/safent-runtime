# Lumen Terminal — writable HOME for the operator's TUI session.
# ostree/bootc resets ownership at boot; tmpfiles re-asserts it (same pattern as
# hermes.conf). The TUI (Textual) writes config/cache here under $HOME.
d /var/lib/hermes/tui-home 0700 hermes-user hermes - -
